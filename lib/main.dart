// import a helper lib from flutter to get content on the screen
import 'package:flutter/material.dart';
import 'src/app.dart';

// define a 'main' function, and run it
void main() => runApp(AppWidget());
